spring:
  application:
    name: online-store

  datasource:
    url: jdbc:postgresql://localhost:5433/online_store_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081